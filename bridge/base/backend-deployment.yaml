#! backend-deployment.yaml
# Generated code, do not edit
apiVersion: apps/v1
kind: Deployment
metadata:
    name: backend
    namespace: infrastructure-as-code-cloud-scale-project
    labels:
        com.docker.compose.project: infrastructure-as-code-cloud-scale-project
        com.docker.compose.service: backend
spec:
    replicas: 1
    selector:
        matchLabels:
            com.docker.compose.project: infrastructure-as-code-cloud-scale-project
            com.docker.compose.service: backend
    strategy:
        type: Recreate
    template:
        metadata:
            labels:
                com.docker.compose.project: infrastructure-as-code-cloud-scale-project
                com.docker.compose.service: backend
                com.docker.compose.network.app-network: "true"
        spec:
            containers:
                - name: backend-container
                  image: infrastructure-as-code-cloud-scale-project-backend
                  imagePullPolicy: IfNotPresent
                  ports:
                    - name: backend-3000
                      containerPort: 3000
